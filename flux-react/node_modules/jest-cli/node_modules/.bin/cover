#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cover/bin/cover" "$@"
  ret=$?
else 
  node  "$basedir/../cover/bin/cover" "$@"
  ret=$?
fi
exit $ret
